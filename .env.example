# FRED (Federal Reserve Economic Data) APIキー
# 取得先: https://fred.stlouisfed.org/docs/api/api_key.html (無料登録・即時発行)
#
# 使い方: このファイルを .env という名前でコピーし、値を設定してください。
#   cp .env.example .env   (Windowsの場合は copy .env.example .env)
FRED_API_KEY=
