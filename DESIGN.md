# DESIGN.md — マクロ経済ダッシュボード

## 0. 実装アーキテクチャ方針(最重要)

このサイトは引き続き **Streamlitアプリ**([app.py](app.py) + `views/*.py`)として実装する。ただし以下の1点だけ例外を設ける。

- **ヒーローだけ**、`st.components.v1.html()` で埋め込む独立したHTML/CSS/JSブロックにする。Streamlitはユーザー操作のたびにPythonスクリプトを丸ごと再実行する仕組みのため、素の `st.markdown` では常駐するJSアニメーションを維持できない。iframeとして切り出すことで、この中だけ本物のJS(SVG/Canvas描画、視差、`IntersectionObserver`)を動かせる。
- **ヒーロー以外(市場データサマリ、政策金利・CPI・失業率・ドル円・株価Indexの各推移ページ、管理者ページ)は既存のStreamlitネイティブ構成のまま**で、見た目だけヒーローに合わせる。カラー・タイポグラフィ・罫線ルールを共通CSSとして各ページ冒頭で注入し、サイト全体のトーンを統一する。独自のSPAルーティング・OGP制御・ページ内アンカーによるセクション横断ナビは、この構成にはそぐわない部分があるため後述の各章で個別に翻訳する。

対象指標は既存の [src/config.py](src/config.py) `SERIES` の5指標(政策金利・CPI・失業率・ドル円・株価Index)のみ。新規指標(TOPIX・NASDAQ・米国10年国債利回り等)は今回のスコープに含めない。

## 1. デザインの目的

日本とアメリカの主要マクロ指標を、専門家向け端末のような緊張感と、初見でも意味が伝わる明快さの両方を備えた1画面にまとめる。

最初の画面(ヒーロー)では「何のサイトか」を印象的に、動きのあるビジュアルで伝え、その直下で最新値を比較できるようにする。ヒーローはブランド体験、ダッシュボードは情報取得に役割を分ける。

## 2. 参考サイトから採用する要素

### FUNTECH Brand Identity から

- 画面全体を一枚のグラフィックとして見せる大胆なファーストビュー
- 黒を基調に、強いアクセントカラーと大きなタイポグラフィを重ねる構成
- 細い罫線、座標・番号・短いラベルによる「操作盤」のようなディテール
- 奥行きのあるオブジェクトと、背景を横断する軌道線による運動感

### NOT HERE から

- 大きな数字を主役にした、余白の広いグリッド
- 罫線だけで領域を区切り、カードの装飾を増やしすぎない配置
- 見出し、数値、単位、補足情報の明快な階層
- 一覧を見ただけで複数指標を比較できる情報密度

### このサイト独自の翻訳

参考サイトの表現をそのまま模倣せず、マクロ経済に合わせて「データの軌道」をビジュアルモチーフにする。派手な3Dキャラクターの代わりに、日本と米国のデータ点、折れ線、グリッド、地球の曲率を思わせる抽象形状を使う。

## 3. デザインコンセプト

**MACRO ORBIT — 数字の変化から、経済の現在地を読む。**

キーワード: `Editorial` / `Data Terminal` / `Bold Typography` / `Japan vs US` / `Motion`

サイト全体は「経済指標が軌道上を動き続ける観測画面」として設計する。日本を朱赤、米国を電気的なブルーで表し、両国を色だけに依存せず `JP` / `US` ラベルでも識別する。

## 4. ページ構成

実際のページ単位は [app.py](app.py) の `st.navigation` にそのまま対応する。

1. **市場データサマリ** ([views/home.py](views/home.py)) — ヒーロー(JS埋め込み) + 市場データサマリ(5指標のグリッド)
2. **政策金利の推移** ([views/policy_rate.py](views/policy_rate.py))
3. **消費者物価指数の推移** ([views/cpi.py](views/cpi.py))
4. **失業率の推移** ([views/unemployment.py](views/unemployment.py))
5. **ドル円の推移** ([views/fx.py](views/fx.py))
6. **株価Indexの推移** ([views/stock_index.py](views/stock_index.py))
7. **管理者ページ** ([views/admin.py](views/admin.py)、サイドバー最後尾、通常の閲覧動線から独立)

ヒーローがあるのは市場データサマリ(トップページ)のみ。2〜6のページはヒーローを持たず、共通CSSによる配色・タイポグラフィだけを継承した「静かなバージョン」にする(既存のタイトル・戻るリンク・グラフ構成は変更しない)。7の管理者ページはトーンも含めて現状維持でよい(操作画面であり、ブランド表現より視認性・誤操作防止を優先する)。

## 5. ヒーローセクション(市場データサマリのみ)

### レイアウト

- 画面幅いっぱい、高さは `st.components.v1.html(..., height=...)` で固定値指定(目安: デスクトップ640px、モバイル480px。Streamlitのcomponentsはコンテナに応じた可変高にできないため、ブレークポイントごとに呼び出し側で高さを出し分ける)
- 中央から左寄りに巨大な2行タイトル `MACRO / SIGNALS`
- 左上に小さく `JAPAN / UNITED STATES · DAILY MACRO VIEW`
- タイトル右下に短い説明文と「指標を見る ↓」CTA(同一ページ内の市場データサマリ見出しへスクロールする通常のアンカーリンク。Streamlitの見出しは自動でid付与されるため `#market-data-summary` のようなページ内リンクとして実現できる)
- 背景に日本と米国の時系列を抽象化した2本の軌道線とデータ点
- 下端に最終更新日時とデータ状態を配置(`SCROLL TO DATA` のようなスクロール誘導コピーは、ページ内アンカーが実際に機能する範囲でのみ使う)

### ヒーローイメージのアートディレクション

黒に近い背景の中央に、光沢を抑えた立体的な「データ・オーブ」を複数浮かべる。オーブは球体や角の丸い小石状で、表面には `%`、`¥`、`$`、上向き/下向き矢印、`CPI`、`RATE` などの記号を小さく刻む。

朱赤の軌道は日本、ブルーの軌道は米国を表し、オーブ間を横切る。背景には薄い座標グリッドと等高線を入れ、経済の「流れ」と「現在地」を示す。画像内にはサイトタイトルや長文を焼き込まず、文字はHTMLで重ねる。

実装は静止画像ではなく、**SVGまたはCanvasによるコード生成**を基本とする(軌道線の描画アニメーション・オーブの緩い浮遊アニメーションをJSで直接制御するため)。外部画像ファイルへの依存を避け、iframe内で完結させる。

### ヒーローコピーの見え方

- `MACRO SIGNALS` はビューポート幅に応じて `clamp(4rem, 14vw, 13rem)`
- 行間は0.82〜0.9、字間はやや詰める
- 日本語説明は最大28〜34文字/行、2〜3行
- CTAは塗りつぶしではなく、細い枠+矢印。ホバー時に背景色が反転

### ヒーローの技術メモ

- `st.components.v1.html(html, height=640, scrolling=False)` で埋め込む。CSS/JSはこのHTML文字列内に完結させ、外部Streamlitの状態(`st.session_state`等)には依存させない(iframeはStreamlitの再実行と切り離されているため)。
- 最終更新日時など動的な値は、Python側でDBから取得した値をこのHTML文字列に埋め込んでレンダリングする(f-stringやテンプレートでの差し込み)。
- `prefers-reduced-motion: reduce` を検知した場合、軌道線描画とオーブの浮遊アニメーションを停止し、静止したレイアウトにフォールバックする。

## 6. 他ページへの視覚言語の適用

ヒーロー以外のページ(市場データサマリの下半分、各推移ページ)は、以下を **共通CSS** として各ページ冒頭で1回だけ注入し、統一したトーンにする。

- 背景色・文字色・罫線色は第9章のカラーパレットに準拠
- 数値表示は `font-variant-numeric: tabular-nums` を指定したフォントに統一
- 日本=朱赤・米国=ブルーのアクセントは、各推移ページの国別 `st.metric` やグラフの色にも適用する(現状Plotlyのデフォルト配色になっている折れ線グラフの色を、日本=朱赤・米国=ブルーに変更する)
- カード風の角丸+ドロップシャドウは使わず、`st.container(border=True)` の代わりに罫線ベースの区切りに置き換える

共通CSSは1箇所(例: `src/theme.py` のような共有モジュール)にまとめ、各ページの先頭で呼び出す形を推奨する。ページごとにCSSを重複して書かない。

## 7. ダッシュボードセクション(市場データサマリ)

### 基本レイアウト

- 背景はオフホワイト `#F2F1EC`
- 上部に `MARKET DATA SUMMARY`(市場データサマリ)の見出し、更新日時、凡例
- 指標は5つ(政策金利・CPI・失業率・ドル円・株価Index)。**1指標=1行**とし、行の中で国別セルを罫線区切りで並べる(ドル円のみ国別セルが1つ)
- 各行はカード影を使わず、行の上下を1pxの罫線で区切る
- 各国別セル全体をクリック可能にし、対応する推移ページ(例: `views/policy_rate.py`)へ遷移する。Streamlit上では、セルをカスタムHTMLの `<a target="_self" href="{ページのURLパス}">` で囲むことで実現する

### 指標行の構成(左から)

1. 指標名 + 推移ページへのリンク(既存の「推移を見る →」を踏襲)
2. 国別セル(日本 / アメリカ、ドル円のみ単一): 大きな数値(最も大きい文字)→ 単位 → 国・系列ラベル(直下の小キャプション)→ データ時点(さらに小さいグレー)

数値は `font-variant-numeric: tabular-nums` を指定し、桁位置を揃える。

### 指標行の並び順(既存 `SERIES` 辞書の順序を維持)

| 順 | 指標 | 国別セル |
|---|---|---|
| 1 | 政策金利 | 日本(無担保コールレート翌日物) / アメリカ(FF実効金利) |
| 2 | 消費者物価指数(CPI) | 日本 / アメリカ |
| 3 | 失業率 | 日本(完全失業率) / アメリカ(失業率) |
| 4 | ドル円 | USD/JPY(単一セル) |
| 5 | 株価Index | 日経平均株価 / S&P500 |

### インタラクション

- ホバー: 背景を国別の薄い色(朱赤 or ブルーの極薄トーン)にし、矢印を8px移動
- フォーカス: 2pxの明瞭なアウトライン
- クリック: セル全体で対応する推移ページへ遷移
- 読み込み時(DBが空): 既存の `st.info` 案内文をそのまま使う
- 値が取得できていない国セル: 既存の `col.warning` 文言(「データがありません」)を踏襲

## 8. 各推移ページ(政策金利・CPI・失業率・ドル円・株価Index)

既存の [src/charts.py](src/charts.py) の構成(タイトル → 最新値 → 時系列グラフ → 生データ表 → DB更新状況 → 戻るリンク)は変更しない。デザイン適用範囲は色・タイポグラフィ・罫線のみ。

- タイトル直下の最新値表示(`st.metric` 相当)を、ダッシュボードの指標セルと同じ「大きい数値+直下キャプション」の見た目に揃える
- 折れ線グラフの配色を日本=朱赤・米国=ブルーに統一する
- 期間切替(1M/3M/1Y/5Y/MAX)は現状の `charts.py` には存在しない機能のため、今回のスコープには含めない(将来拡張の候補として別途検討)

## 9. カラー

| 用途 | 色 | HEX |
|---|---|---|
| ヒーロー背景 | Ink Black | `#0B0D0E` |
| 本文背景 | Warm White | `#F2F1EC` |
| 主文字 | Near Black | `#121415` |
| ヒーロー文字 | Soft White | `#F7F6F2` |
| 日本 | Signal Vermilion | `#FF3D18` |
| 米国 | Electric Blue | `#2D5BFF` |
| 補助線 | Grid Gray | `#C9CBC7` |
| 上昇 | Positive Green | `#087A55` |
| 低下 | Negative Red | `#C52A2A` |

上昇が常に「良い」とは限らないため、緑/赤は単純な良し悪しではなく前回差の方向だけに使う。CPIや失業率には文脈ラベルを添える(前回差の算出自体は今回のスコープでは未実装。表示するのは最新値のみで、増減の色分けは将来DBに前回値比較を持たせた際に適用する)。

## 10. タイポグラフィ

- 欧文見出し: `Arial Narrow`、`Roboto Condensed`、または可変幅のGrotesk系
- 日本語: `Noto Sans JP` またはシステムサンセリフ
- 数値: 同系統のタブラー数字対応フォント
- ヒーローは極太、セクション見出しは中太、補足はRegular
- 本文の最小サイズは14px、主要説明は16〜18px

Webフォントが取得できない環境でも崩れないフォールバックを指定する。

## 11. モーション

モーションが「本物のJS」で動くのはヒーロー(iframe内)のみ。それ以外は素のStreamlit DOMなので、以下のように使い分ける。

**ヒーロー(iframe内、本格的なJS/CSSアニメーション)**
- 初回表示: 軌道線が0.8〜1.2秒で描画され、タイトルが短く立ち上がる
- スクロール: オーブに最大16px程度の緩い視差
- `prefers-reduced-motion: reduce` では視差と線描画を停止

**ダッシュボード以降(Streamlitネイティブ、CSSのみのベストエフォート)**
- セルの初回表示フェードインはCSSアニメーションで代替可能だが、Streamlitは操作のたびにDOMを再構築するため、**何もしないと再実行のたびにアニメーションが再生されてしまう**。`st.session_state` に「初回表示済みフラグ」を持たせ、2回目以降のレンダリングではアニメーションクラスを付与しない、という形で1回だけの演出にする
- 常時動く背景、常時ループするアニメーションはダッシュボード側では使わない(データの可読性を優先)

## 12. レスポンシブ

### Desktop(1200px以上)

- ヒーロータイトルを2行で最大化
- ダッシュボードの国別セルは横並び

### Tablet(768〜1199px)

- ヒーローの高さをやや圧縮(iframeの `height` を出し分け)
- 説明文の横幅を40〜50vwにする

### Mobile(767px以下)

- ヒーローのiframe高さを480px程度に縮小
- タイトルは2〜3行
- ダッシュボードの国別セルは縦積みに切り替え(Streamlitの `st.columns` 標準挙動に準拠、罫線も垂直→水平に切り替える)
- タップ領域は最低44×44px

## 13. アクセシビリティ

- WCAG 2.2 AA相当のコントラストを確保
- 装飾画像(ヒーローのオーブ・軌道線)は空のaltまたは `aria-hidden`、意味のある可視化(データ点)には短い代替説明
- 国や増減を色だけで区別しない(`JP`/`US`ラベル、`+`/`−`符号を必ず併記)
- キーボードだけですべての指標セルを操作可能にする(`<a>` タグでラップしているため標準でTab移動可能)
- 見出し階層を `h1 → h2 → h3` の順で保つ
- ヒーロー内でCanvas/SVGを使う場合も、更新日時などの数値情報は必ずHTML(テキストノード)として提供し、Canvas内の図形だけに依存させない

## 14. 実装上の禁止事項

- 参考サイトのロゴ、固有グラフィック、レイアウトをそのまま複製しない
- ダッシュボードを角丸カードとドロップシャドウの集合にしない
- ヒーロー画像内に読ませたい文字(タイトル等)を画像/Canvasに焼き込まず、HTMLで重ねる
- ダミーの最新値を実データのように表示しない
- 常時動く背景、強いグロー、過度なガラス表現で数値の可読性を落とさない
- 既存のStreamlit機能(サイドバーナビゲーション、`st.page_link` による遷移、管理者ページのDB更新ボタン)の動作を壊さない

## 15. 完成判定

- ファーストビュー(ヒーロー)だけで「日米のマクロ経済を比較するサイト」と分かる
- 1回のスクロールで5指標の最新値に到達できる
- 日米の同種指標を視線移動の少ない位置で比較できる
- 主要な数値、更新日、単位が同時に読める
- 360px幅と1440px幅の双方で情報階層が維持される
- ヒーロー以外のページ(推移ページ・管理者ページ)も、既存の機能を保ったまま同じ配色・タイポグラフィで統一されている
