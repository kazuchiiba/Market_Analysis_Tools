# CONTENTS.md — マクロ経済ダッシュボード

[DESIGN.md](DESIGN.md) の実装アーキテクチャ方針(0章)に対応するコンテンツ仕様。ヒーローは市場データサマリ([views/home.py](views/home.py))のみに存在し、それ以外は既存の `views/*.py` ページ構成をそのまま使う。対象指標は既存 [src/config.py](src/config.py) の5指標(政策金利・CPI・失業率・ドル円・株価Index)のみ。

## 1. サイトの役割

日本とアメリカの主要マクロ指標を1画面で把握し、気になる指標から日次の時系列推移へ進めるサイト。投資判断を直接推奨するのではなく、経済の現在地を短時間で確認するための入口とする。

## 2. サイト名・メタ情報

### サイト名

**MACRO SIGNALS**

日本語表記: **日米マクロ経済ダッシュボード**

### ブラウザタブのタイトル

`st.set_page_config(page_title=...)` で各ページに設定する。

| ページ | page_title |
|---|---|
| 市場データサマリ | `市場データサマリ｜MACRO SIGNALS` |
| 政策金利の推移 | `政策金利の推移｜MACRO SIGNALS` |
| 消費者物価指数の推移 | `消費者物価指数の推移｜MACRO SIGNALS` |
| 失業率の推移 | `失業率の推移｜MACRO SIGNALS` |
| ドル円の推移 | `ドル円の推移｜MACRO SIGNALS` |
| 株価Indexの推移 | `株価Indexの推移｜MACRO SIGNALS` |

### meta description / OGP

Streamlitは `<head>` のmeta description・OGP画像を標準機能では制御できない(可能にするには静的ファイル差し替え等の追加対応が必要)。**今回のスコープでは対応しない。** 将来対応する場合の文言候補のみ以下に残す。

- description案: `日本とアメリカの政策金利・CPI・失業率・ドル円・株価指数を1画面で比較できるマクロ経済ダッシュボード。`

## 3. ヘッダー(市場データサマリのみ・ヒーロー内)

実ナビゲーションは既存のStreamlitサイドバー(市場データサマリ / 政策金利 / 消費者物価指数 / 失業率 / ドル円 / 株価Index / 管理者ページ)が担う。ヒーロー内の「ヘッダー」はブランド表示に徹し、独自ナビは持たない。

- ロゴテキスト: `MACRO SIGNALS`
- 補助表記: `JP / US`
- 右側・データ状態: `DATA STATUS · LIVE`(DBに全系列のデータがある場合) / `DATA STATUS · DELAYED`(一部またはすべて未取得の場合)

多言語切替(`JP / EN`)は英語版が存在しないため表示しない。

## 4. ヒーロー(市場データサマリのみ)

### アイブロウ

`JAPAN / UNITED STATES · DAILY MACRO VIEW`

### 見出し

`MACRO`
`SIGNALS`

### リード文

`数字の変化から、経済の現在地を読む。`
`日本とアメリカの主要指標を、ひとつの画面で。`

### CTA

- 主CTA: `最新の指標を見る ↓`
- 遷移先: 同一ページ内の市場データサマリ見出しへのページ内アンカー(別ページやセクションへのSPA遷移ではない)

### 補助情報

- `UPDATED · {YYYY.MM.DD HH:mm}` — DB内の全系列のうち最も新しい最終更新日時。DBが空の場合は `NO DATA YET`
- `SOURCE · FRED (Federal Reserve Economic Data)`

## 5. 市場データサマリ

### セクション見出し

- アイブロウ: `CURRENT SNAPSHOT`
- 見出し: `市場データサマリ`
- 説明: `日米の政策金利・物価・雇用・為替・株価を、最新公表値で比較します。`
- データ更新導線: `🛠️ データを更新する(管理者ページへ)`(既存の `st.page_link` 文言を維持)

### 凡例

- `JP 日本`
- `US アメリカ`
- `最終更新 {YYYY.MM.DD HH:mm}`(指標ごとの最終更新日時、既存の `st.metric` の `help` 相当を常時キャプション表示に変更)

### DBが空のときの案内文

既存文言を維持: `DBにデータがまだありません。まず管理者ページから「DB更新」を実行してください。`

## 6. 指標セルの文言とデータ項目

すべてのセルで次のテンプレートを共通利用する。

```text
{country_label}
{formatted_value} {unit}
データ時点 {observation_date}
```

`formatted_value` と `observation_date` はDB取得値から生成し、本文へ固定値として書かない。前回差・前年差の算出は現状のDB(最新値のみ保存)では行えないため、今回のスコープには含めない。

### 指標一覧(既存 `SERIES` 辞書と1:1対応)

| キー | 指標名 | 国別セル | 単位 | 推移ページ |
|---|---|---|---|---|
| `policy_rate` | 政策金利 | 日本(無担保コールレート翌日物) / アメリカ(FF実効金利) | `%` | `views/policy_rate.py` |
| `cpi` | 消費者物価指数(CPI) | 日本(CPI 全品目、2015年=100) / アメリカ(CPI 全項目、季節調整済、1982-84年=100) | 指数 | `views/cpi.py` |
| `unemployment` | 失業率 | 日本(完全失業率) / アメリカ(失業率) | `%` | `views/unemployment.py` |
| `fx` | ドル円 | USD/JPY(国別セル分割なし、単一セル) | 円 | `views/fx.py` |
| `index` | 株価Index | 日経平均株価 / S&P500 | pt | `views/stock_index.py` |

政策金利はFREDから単一の実効値(レンジではない)として取得しているため、`{lower}%–{upper}%` のようなレンジ表示は行わない。

値が取得できていない国セルの文言: `データがありません`(既存の `col.warning` を踏襲)。

## 7. 各推移ページ

遷移先は上表の既存ファイル(`views/policy_rate.py` 等)。URLパスの独自設計(`/indicators/{id}`)は行わない。

### ページ見出し

- H1: `📈 {指標名}の推移`(既存の `st.title` 文言をそのまま使用)
- キャプション: `データ出典: FRED (Federal Reserve Economic Data)。表示内容はローカルDBの保存値です。`(既存文言を維持)
- 最新値: `{formatted_value} {unit}`(国別セルごと)

### 期間切替

現状の [src/charts.py](src/charts.py) は全期間のグラフを表示するのみで、期間切替(1M/3M/1Y/5Y等)は実装されていない。**今回のスコープには含めない。** 将来追加する場合の候補ラベルのみ残す: `1M` / `3M` / `1Y` / `5Y` / `MAX`。

### 補足要素(既存のまま)

- 生データ表(expander: `生データを表示`)
- DB更新状況(expander: `DB更新状況`、系列ごとの最終更新日時・エラー)
- 戻る導線: `← 市場データサマリに戻る`

## 8. データ出典セクション

全指標が単一の出典(FRED)であるため、指標ごとに提供機関を出し分ける複雑な表示は行わない。

### 見出し

`データについて`

### 本文

`本サイトの数値はすべて FRED (Federal Reserve Economic Data) から取得しています。日本の指標(政策金利・CPI・失業率)はOECD経由でFREDに収録されているため、日本銀行や総務省統計局の公表値と更新タイミング・改定有無が異なる場合があります。`

`表示内容は情報提供を目的としたもので、投資・売買・金融取引を推奨するものではありません。重要な判断には各提供元の原典をご確認ください。`

### 出典表示のルール

各推移ページの「DB更新状況」に以下を表示する(既存実装のまま)。

- 系列ごとの最終更新日時
- 最終更新に失敗した場合はエラー内容

FRED系列IDそのものは画面上には出さず、[README.md](README.md) の対応表を参照する形にする(既存方針を維持)。

## 9. 状態別コピー

### 読み込み中

`最新データを取得しています`(管理者ページのDB更新ボタン押下時、既存の `st.spinner` 文言に準拠)

### 一部取得失敗

`一部の指標を更新できませんでした。取得済みの最終値を表示しています。`

### 値なし

- 値: `—`
- 補足: `データがありません`(既存文言)

### DBが空

`DBにデータがまだありません。まず管理者ページから「DB更新」を実行してください。`(既存文言)

## 10. フッター(市場データサマリのみ)

### 左側

- `MACRO SIGNALS`
- `JAPAN / UNITED STATES`

### 右側

- `GITHUB`(公開リポジトリ `github.com/kazuchiiba/Market_Analysis_Tools` へのリンク。単なる参照リンクであり、pushはユーザーの明示指示があるときのみという既存方針に変更はない)

### コピーライト

`© {YYYY} MACRO SIGNALS`

## 11. 表記ルール

- 日本語を主言語にし、英語はセクションラベルと国コードに限定
- 数値は原則として小数第2位まで(既存の `f"{value:,.2f}{unit}"` 表示に準拠)
- 桁区切りにはカンマを使用
- 日付は本文で `2026年8月11日`、省スペース箇所で `2026.08.11`
- 時刻表記は既存実装に合わせてローカル時刻とし、タイムゾーン表記(JST等)は付けない(現状DBがタイムゾーン情報を保持していないため)
- `最新` は取得時刻ではなく、各系列の最新公表値(データ時点)を意味する
- 前回差・前年差・推計値/速報値/改定値の区別は、現状のDBが最新値のみを保持しているため今回は扱わない(将来DBに履歴比較の仕組みを追加した場合の拡張候補)

## 12. 実装時に確定する項目

- ヒーローのiframe高さ(デスクトップ/モバイルの具体的px値)
- 共通CSS注入モジュールの置き場所(例: `src/theme.py`)をどう設計するか
- 「初回表示済みフラグ」を `st.session_state` のどのキー名で管理するか
- ヒーロー内の最終更新日時をPython側からどう安全にHTML文字列へ埋め込むか(エスケープ処理)
- meta description/OGPの将来対応要否
